services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=auction-inventory'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=auction'
    ports:
      - '3306:3306'